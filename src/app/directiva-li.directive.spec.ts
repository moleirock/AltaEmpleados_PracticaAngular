import { DirectivaLiDirective } from './directiva-li.directive';

describe('DirectivaLiDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaLiDirective();
    expect(directive).toBeTruthy();
  });
});
